# Copy to `.env` and set values locally (never commit real keys).

NVIDIA_API_KEY=
MONGO_URI=mongodb://localhost:27017

# Optional:
# NVIDIA_MODEL=nvidia/nemotron-3-nano-omni-30b-a3b-reasoning
# NVIDIA_KEYWORDS_MODEL=
# NVIDIA_ANALYST_MAX_TOKENS=65536
# NVIDIA_ENABLE_THINKING=1

# API-first trend ingestion (SerpApi Google Trends; no scraping):
# SERPAPI_API_KEY=
# INGEST_SEED_KEYWORDS=streetwear,gadgets,home decor
# GOOGLE_TRENDS_GEO=PH
# INGEST_WEBHOOK_TOKEN=  # if set, required on X-Ingest-Token for /api/ingest/* hooks
